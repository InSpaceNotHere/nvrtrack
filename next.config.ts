import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Hide the Next.js dev indicator so it does not overlap the mobile bottom nav during UI review.
  devIndicators: false,
};

export default nextConfig;
